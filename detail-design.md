# データ利活用基盤 詳細設計書

## 1. はじめに
### 1.1. 目的・対象範囲
* 本書の目的と対象スコープ
### 1.2. 参照する基本設計書
* インプットとなった基本設計ドキュメントのバージョンと名称の明記

## 2. 基盤パラメータ・構成詳細
### 2.1. ワークスペース設定値
* Databricksワークスペースの各種パラメータ設定値
### 2.2. ネットワーク・コンピュート構成
* VPC/VCN、Private Link、セキュア・クラスター・コネクティビティ（SCC）の詳細設定
* Serverless SQL Warehouse、汎用クラスターの自動停止時間、スペック選定基準

## 3. カタログ・スキーマ構造定義
### 3.1. 作成対象カタログ一覧
* 環境ごとに実際に作成する全カタログの物理名、用途、管理者一覧
### 3.2. 作成対象スキーマ一覧
* 各カタログ配下に作成する全スキーマの物理名、用途一覧
### 3.3. メタデータ（タグ・コメント）付与ルール
* Unity Catalogオブジェクトに対する `COMMENT` および `TAG` の自動・手動付与ルール

## 4. セキュリティ・権限マッピング詳細
### 4.1. IdPグループ・ロールマッピング
* 同期対象のグループ（例: `grp_jp_analyst`, `grp_global_admin`）の定義
### 4.2. 権限マトリクス（カタログ/スキーマ単位）
* どのグループにどのオブジェクトの何の権限（SELECT, MODIFY等）を付与するかのCRUD/◯×表
### 4.3. 行フィルタ（Row Filter）・列マスク（Column Mask）実装仕様
* 秘匿情報マスク用・会社分離用のSQLユーザー定義関数（UDF）の具体的なコード仕様
### 4.4. 外部共有（Delta Sharing）オブジェクト定義
* 共有対象のパブリッシャー（Share）と受信者（Recipient）の設定・認証情報管理詳細

## 5. メタデータ駆動型パイプライン設計
### 5.1. 共通取り込み・変換フレームワーク仕様
* PySpark / Delta Live Tablesを用いた、構成ファイルを読み込んで動的に動く共通プログラムの構造
### 5.2. パイプライン構成ファイル（JSON/YAML）のスキーマ定義
* ソースDB名、接続情報、対象テーブル名、主キー、差分更新キー等を定義するパラメータファイルの定義仕様
### 5.3. 秘匿情報マッピング・マスキング設定
* 構成ファイル内で「どのカラムに、どの共通マスク関数を適用するか」を指定するための定義方法

## 6. ジョブ・スケジュール詳細
### 6.1. Databricks Workflows（ジョブ）構造
* 5つのソースシステムごとのジョブネット（タスク依存関係）の定義
### 6.2. スケジュール・トリガー設定
* 各ジョブの実行スケジュール（Cron）、前処理・後処理のトリガー条件
### 6.3. リトライ・通知設定詳細
* タスク失敗時のリトライ回数、インターバル、Slack/Teams/メール等への通知宛先設定

## 7. 運用・監査詳細
### 7.1. システムテーブル（System Tables）クエリ仕様
* コスト監視（Billing）、監査（Audit Logs）、データリネージ（Lineage）を確認するための具体的なSQLクエリ仕様
### 7.2. メンテナンス・最適化スケジュール
* Deltaテーブルのパフォーマンスを維持するための `OPTIMIZE` および `VACUUM` の実行周期

## 8. テスト・移行仕様
### 8.1. テスト観点（単体・結合・権限）
* 動的マスクや行フィルターが正しく機能しているかを検証するための権限テストパターン
### 8.2. 初期データ移行（バルクロード）手順
* ソースDB（数百テーブル分）から初回データを一括抽出し、Databricksへ取り込む際の具体的な実行手順

## 付録
* 命名規約詳細（カラム・ジョブ等の詳細ルール）
* 共通エラーコードと対応手順
* 未決事項一覧